
// AetTree
GEOMETRY.AetTree = function() {
	this.top_node = null; // edge node
};

GEOMETRY.AetTree.prototype = {
	print: function() {
		//for (var edge = this.top_node; edge != null; edge = edge.next) {
		//	console.log('edge.vertex.x='+ edge.vertex.x +'  edge.vertex.y='+ edge.vertex.y);
		//}
	}
};
